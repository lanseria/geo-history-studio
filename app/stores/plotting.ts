// app/stores/plotting.ts
import { defineStore } from 'pinia'

export const usePlottingStore = defineStore('plotting', () => {
  // TODO:
  // - State: drawnEntities, activeTool, isDrawing
  // - Actions: startDrawing(toolType), stopDrawing, saveEntity, removeEntity, updateEntityStyle

  return {}
})
